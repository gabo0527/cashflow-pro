'use client'
import PlaceholderPage from '@/components/ui/PlaceholderPage'

export default function AssumptionsPage({ theme }: { theme?: 'light' | 'dark' }) {
  return (
    <PlaceholderPage
      title="Assumptions"
      description="Manage forecast inputs and scenarios"
      theme={theme}
    />
  )
}
